import React from 'react'

function Home() {
  return (
    
    // // import React, { useState, useEffect } from 'react';
    // import './profile.css';
    // import axios from 'axios';
    // import Navbar from './Navbar';
    
    // function Profile() {
    //     const [userProfile, setUserProfile] = useState(null);
    
    //     useEffect(() => {
    //         // Replace 'your-backend-api-url' with the actual URL of your backend API
    //         const apiUrl = 'http://localhost:5000/api/profile';
    
    //         // Replace 'prasadbagyya1997@gmail.com' with the actual email of the logged-in user
    //         const userEmail = 'isuru@gmail.com';
    
    //         axios.post(apiUrl, { email: userEmail })
    //             .then(response => {
    //                 // Assuming the response contains user data directly or nested within 'data'
    //                 const userData = response.data?.user || response.data;
    
    //                 if (userData) {
    //                     // Set the user profile data in the state
    //                     setUserProfile(userData);
    //                 } else {
    //                     // Handle error, e.g., show a message to the user
    //                     console.log('Invalid response structure:', response.data);
    //                 }
    //             })
    //             .catch(error => {
    //                 // Handle error, e.g., show a message to the user
    //                 console.error('Error fetching user profile:', error.message);
    //             });
    //     }, []); // The empty dependency array ensures the effect runs only once on mount
    
    //     return (
    //         <div>
    //             {userProfile ? (
    //                 <div>
    //                     <h2>User Profile</h2>
    //                     <p>Name: {userProfile.name}</p>
    //                     <p>Email: {userProfile.email}</p>
    //                 </div>
    //             ) : (
    //                 <p>Loading...</p>
    //             )}
    //         </div>
    //     );
    // }
    
    // export default Profile;
    
    <></>

  )
}

export default Home